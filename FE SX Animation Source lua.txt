local gui = Instance.new("ScreenGui")
gui.Name = "patrickGui"
gui.Parent = game.CoreGui

--///patrick gui maker///---



--[[

credit: Animatrix

Open Source
    ____________   _____ _______  __    ________  ______
   / ____/ ____/  / ___// ____/ |/ /   / ____/ / / /  _/
  / /_  / __/     \__ \/ __/  |   /   / / __/ / / // /  
 / __/ / /___    ___/ / /___ /   |   / /_/ / /_/ // /   
/_/   /_____/   /____/_____//_/|_|   \____/\____/___/   
                                                        

updatelog:
 ├bang v2
 ├front v3
 ├front v4
 └blowjob v1
  └=4>04

]]


local function corner(ui,val)
        local UICorner = Instance.new("UICorner")
        UICorner.CornerRadius = UDim.new(val)
        UICorner.Parent = ui
end 

local function st(as,s,a,b,c)
        local rizz = Instance.new("UIStroke")
        rizz.Color = Color3.new(a, b, c)
        rizz.Thickness = s
        rizz.Parent = as
end

local frame = Instance.new("Frame")
frame.Size = UDim2.new(0.2, 0, 0.05, 0)
frame.Position = UDim2.new(0.2, 0, 0, 0)
frame.BackgroundColor3 = Color3.new(0.5, 0, 0)
frame.BorderColor3 = Color3.new(0, 0, 0)
frame.BorderSizePixel = 0
frame.Active = true
frame.BackgroundTransparency = 0 
frame.Draggable = true
frame.Parent = gui

corner(frame,1)
st(frame,3,0.8,0.3,0)

local label_lol = Instance.new("TextLabel")
label_lol.Size = UDim2.new(1, 0, 0.8, 0)
label_lol.Position = UDim2.new(0, 0, 0.1, 0)
label_lol.BackgroundColor3 = Color3.new(0, 0, 0)
label_lol.BorderColor3 = Color3.new(0, 0, 0)
label_lol.BorderSizePixel = 0
label_lol.Text = "[FE] SEX GUI V1.04"
label_lol.TextColor3 = Color3.new(255, 255, 255)
label_lol.BackgroundTransparency = 1
label_lol.Font = Enum.Font.FredokaOne
label_lol.TextScaled = true
label_lol.Parent = frame

local textbutton = Instance.new("TextButton")
textbutton.Size = UDim2.new(0.12, 0, 1, 0)
textbutton.Position = UDim2.new(0.88, 0, 0, 0)
textbutton.BackgroundColor3 = Color3.new(1, 0.5, 0)
textbutton.BorderColor3 = Color3.new(0, 0, 0)
textbutton.BorderSizePixel = 0
textbutton.Text = "–"
textbutton.TextColor3 = Color3.new(255, 255, 255)
textbutton.BackgroundTransparency = 0
textbutton.Font = Enum.Font.FredokaOne
textbutton.TextScaled = true
textbutton.Parent = frame

corner(textbutton,1)

local frame2 = Instance.new("Frame")
frame2.Size = UDim2.new(1, 0, 10, 0)
frame2.Position = UDim2.new(0, 0, 1, 0)
frame2.BackgroundColor3 = Color3.new(0.3,0,0)
frame2.BorderColor3 = Color3.new(0, 0, 0)
frame2.BorderSizePixel = 0
frame2.Active = true
frame2.BackgroundTransparency = 0
frame2.Parent = frame

corner(frame2,0.05)
st(frame2,3,0.8,0.3,0)

local scol = Instance.new("ScrollingFrame")
scol.Size = UDim2.new(0.8, 0, 0.8, 0)
scol.Position = UDim2.new(0.1, 0, 0.1, 0)
scol.BackgroundColor3 = Color3.new(1, 1, 1)
scol.BorderColor3 = Color3.new(0, 0, 0)
scol.BorderSizePixel = 0
scol.Parent = frame2
scol.CanvasSize = UDim2.new(0,0,20,0)
scol.ScrollBarThickness = 0
scol.BackgroundTransparency = 1

local UIListLayout = Instance.new("UIListLayout")
UIListLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
UIListLayout.SortOrder = Enum.SortOrder.LayoutOrder
UIListLayout.Padding = UDim.new(0, 10)
UIListLayout.Parent = scol

local namebox = Instance.new("TextBox")
namebox.Size = UDim2.new(0.8, 0, 0.1, 0)
namebox.Position = UDim2.new(0.1, 0, 0.9, 0)
namebox.BackgroundColor3 = Color3.new(0.2, 0, 0)
namebox.BorderColor3 = Color3.new(0, 0, 0)
namebox.BorderSizePixel = 0
namebox.Text = ""
namebox.PlaceholderText = "Enter Name or Display Name"
namebox.TextColor3 = Color3.new(1,1,1)
namebox.BackgroundTransparency = 0
namebox.Font = Enum.Font.FredokaOne
namebox.TextScaled = true
namebox.TextSize = 20
namebox.TextWrapped = true
namebox.Parent = frame2

corner(namebox, 1)

local front = Instance.new("TextButton")
front.Size = UDim2.new(0.4, 0, 0.1, 0)
front.Position = UDim2.new(0.1, 0, 0, 0)
front.BackgroundColor3 = Color3.new(0.8, 0.4, 0)
front.BorderColor3 = Color3.new(0, 0, 0)
front.BorderSizePixel = 0
front.Text = "front player"
front.TextColor3 = Color3.new(255, 255, 255)
front.BackgroundTransparency = 0
front.Font = Enum.Font.FredokaOne
front.TextScaled = true
front.Parent = frame2

corner(front, 0.4)

local behind = Instance.new("TextButton")
behind.Size = UDim2.new(0.4, 0, 0.1, 0)
behind.Position = UDim2.new(0.5, 0, 0, 0)
behind.BackgroundColor3 = Color3.new(0.8, 0.4, 0)
behind.BorderColor3 = Color3.new(0, 0, 0)
behind.BorderSizePixel = 0
behind.Text = "behind player"
behind.TextColor3 = Color3.new(255, 255, 255)
behind.BackgroundTransparency = 0
behind.Font = Enum.Font.FredokaOne
behind.TextScaled = true
behind.Parent = frame2

corner(behind, 0.4)

local front2 = Instance.new("TextButton")
front2.Size = UDim2.new(0.1, 0, 0.1, 0)
front2.Position = UDim2.new(0, 0, 0, 0)
front2.BackgroundColor3 = Color3.new(0.8, 0.4, 0)
front2.BorderColor3 = Color3.new(0, 0, 0)
front2.BorderSizePixel = 0
front2.Text = "Front v2"
front2.TextColor3 = Color3.new(255, 255, 255)
front2.BackgroundTransparency = 0
front2.Font = Enum.Font.FredokaOne
front2.TextScaled = true
front2.Parent = frame2
corner(front2, 0.3)

local player = game.Players.LocalPlayer
local teleportSpeed = 10
local isTeleporting = false
local targetPlayer = nil

local function getTargetPlayerByName(name, cfm)
    for _, p in ipairs(game.Players:GetPlayers()) do
        if p.Name:sub(1, #name) == name or p.DisplayName:sub(1, #name) == name then
            return p
        end
    end
    return nil
end

local function startTeleport()
    if targetPlayer and targetPlayer.Character then
        local targetCharacter = targetPlayer.Character
        local targetRootPart = targetCharacter:FindFirstChild("HumanoidRootPart")
        
        if targetRootPart then
            local offset = CFrame.new(0, 1.1, 1)
           
            local teleportLoop
            teleportLoop = game:GetService("RunService").Stepped:Connect(function()
                if isTeleporting then
                    player.Character:SetPrimaryPartCFrame(targetRootPart.CFrame * offset)
                else
                    teleportLoop:Disconnect()
                end
            end)
        end
    end
end

behind.MouseButton1Click:Connect(function()
    local targetName = namebox.Text
    targetPlayer = getTargetPlayerByName(targetName)

    if isTeleporting then
        isTeleporting = false
        behind.Text = "behind player"
    else
        isTeleporting = true
        behind.Text = "Stop Teleport"
        startTeleport()
    end
end)

local player = game.Players.LocalPlayer
local teleportSpeed = 10
local isTeleporting = false
local targetPlayer = nil

local function getTargetPlayerByName(name, cfm)
    for _, p in ipairs(game.Players:GetPlayers()) do
        if p.Name:sub(1, #name) == name or p.DisplayName:sub(1, #name) == name then
            return p
        end
    end
    return nil
end

local function startTeleport()
    if targetPlayer and targetPlayer.Character then
        local targetCharacter = targetPlayer.Character
        local targetRootPart = targetCharacter:FindFirstChild("HumanoidRootPart")
        
        if targetRootPart then
            local offset = CFrame.new(0, 1.1, -2)
           
            local teleportLoop
            teleportLoop = game:GetService("RunService").Stepped:Connect(function()
                if isTeleporting then
                    player.Character:SetPrimaryPartCFrame(targetRootPart.CFrame * offset)
                else
                    teleportLoop:Disconnect()
                end
            end)
        end
    end
end

front.MouseButton1Click:Connect(function()
    local targetName = namebox.Text
    targetPlayer = getTargetPlayerByName(targetName)

    if isTeleporting then
        isTeleporting = false
        front.Text = "front player"
    else
        isTeleporting = true
        front.Text = "Stop Teleport"
        startTeleport()
    end
end)

local player = game.Players.LocalPlayer
local teleportSpeed = 10
local isTeleporting = false
local targetPlayer = nil
local offset = CFrame.new(0, 1.1, -2)
local toggleInterval = 0.3

local function getTargetPlayerByName(name, cfm)
    for _, p in ipairs(game.Players:GetPlayers()) do
        if p.Name:sub(1, #name) == name or p.DisplayName:sub(1, #name) == name then
            return p
        end
    end
    return nil
end

local function startTeleport()
    if targetPlayer and targetPlayer.Character then
        local targetCharacter = targetPlayer.Character
        local targetRootPart = targetCharacter:FindFirstChild("HumanoidRootPart")
        
        if targetRootPart then
            local teleportLoop
            teleportLoop = game:GetService("RunService").Stepped:Connect(function()
                if isTeleporting then
                    player.Character:SetPrimaryPartCFrame(targetRootPart.CFrame * offset)
                else
                    teleportLoop:Disconnect()
                end
            end)

            -- Toggle offset every 'toggleInterval' seconds
            local lastToggleTime = tick()
            local toggleOffset
            toggleOffset = game:GetService("RunService").Heartbeat:Connect(function()
                if isTeleporting then
                    if tick() - lastToggleTime >= toggleInterval then
                        -- Toggle the offset
                        if offset == CFrame.new(0, 1.1, -2) then
                            offset = CFrame.new(0, 1.1, -3)
                        else
                            offset = CFrame.new(0, 1.1, -2)
                        end
                        lastToggleTime = tick() -- Update the time of the last toggle
                    end
                else
                    toggleOffset:Disconnect()
                end
            end)
        end
    end
end

front2.MouseButton1Click:Connect(function()
    local targetName = namebox.Text
    targetPlayer = getTargetPlayerByName(targetName)

    if isTeleporting then
        isTeleporting = false
        front2.Text = "Front v2"
    else
        isTeleporting = true
        front2.Text = "Stop"
        startTeleport()
    end
end)

local front3 = Instance.new("TextButton")
front3.Size = UDim2.new(0.1, 0, 0.1, 0)
front3.Position = UDim2.new(0, 0, 0.1, 0)
front3.BackgroundColor3 = Color3.new(0.8, 0.4, 0)
front3.BorderColor3 = Color3.new(0, 0, 0)
front3.BorderSizePixel = 1
front3.Text = "Front v3"
front3.TextColor3 = Color3.new(255, 255, 255)
front3.BackgroundTransparency = 0
front3.Font = Enum.Font.FredokaOne
front3.TextScaled = true
front3.Parent = frame2
corner(front3, 0.3)

local player = game.Players.LocalPlayer
local teleportSpeed = 10
local isTeleporting = false
local targetPlayer = nil
local offset = CFrame.new(0, 1.1, -2)
local toggleInterval = 0.3

local function getTargetPlayerByName(name)
    for _, p in ipairs(game.Players:GetPlayers()) do
        if p.Name:sub(1, #name) == name or p.DisplayName:sub(1, #name) == name then
            return p
        end
    end
    return nil
end

local function startTeleport()
    if targetPlayer and targetPlayer.Character then
        local targetCharacter = targetPlayer.Character
        local targetRootPart = targetCharacter:FindFirstChild("HumanoidRootPart")
        
        if targetRootPart then
            local teleportLoop
            teleportLoop = game:GetService("RunService").Stepped:Connect(function()
                if isTeleporting then
                    -- Teleport player while rotating them to face backward
                    local targetCFrame = targetRootPart.CFrame * offset
                    player.Character:SetPrimaryPartCFrame(targetCFrame * CFrame.Angles(0, math.rad(180), 0))
                else
                    teleportLoop:Disconnect()
                end
            end)

            -- Toggle offset every 'toggleInterval' seconds
            local lastToggleTime = tick()
            local toggleOffset
            toggleOffset = game:GetService("RunService").Heartbeat:Connect(function()
                if isTeleporting then
                    if tick() - lastToggleTime >= toggleInterval then
                        if offset == CFrame.new(0, 1.1, -2) then
                            offset = CFrame.new(0, 1.1, -3)
                        else
                            offset = CFrame.new(0, 1.1, -2)
                        end
                        lastToggleTime = tick()
                    end
                else
                    toggleOffset:Disconnect()
                end
            end)
        end
    end
end

front3.MouseButton1Click:Connect(function()
    local targetName = namebox.Text
    targetPlayer = getTargetPlayerByName(targetName)

    if isTeleporting then
        isTeleporting = false
        front3.Text = "Front v3"
    else
        isTeleporting = true
        front3.Text = "Stop"
        startTeleport()
    end
end)

textbutton.MouseButton1Click:Connect(function()
        frame2.Visible = not frame2.Visible
        if frame2.Visible then
                textbutton.BackgroundColor3 = Color3.new(1, 0.5, 0)
        else
                textbutton.BackgroundColor3 = Color3.new(0.5, 0.5, 0.5)
        end
end)

local function se(nam, Ida, Idb, Idc, Idd, duration,sp)
        local isPlaying = false
        local humanoid = game.Players.LocalPlayer.Character:WaitForChild("Humanoid")
        local animations = {
                {Id = "rbxassetid://" .. Ida, Track = nil},
                {Id = "rbxassetid://" .. Idb, Track = nil},
                {Id = "rbxassetid://" .. Idc, Track = nil},
                {Id = "rbxassetid://" .. Idd, Track = nil}
        }

        local animationButton = Instance.new("TextButton")
        animationButton.Size = UDim2.new(1, 0, 0.005, 0)
        animationButton.Position = UDim2.new(0.1, 0, 0.1, 0)
        animationButton.BackgroundColor3 = Color3.new(1, 0.5, 0)
        animationButton.BorderColor3 = Color3.new(0, 0, 0)
        animationButton.BorderSizePixel = 0
        animationButton.Text = nam
        animationButton.TextColor3 = Color3.new(255, 255, 255)
        animationButton.BackgroundTransparency = 0
        animationButton.Font = Enum.Font.FredokaOne
        animationButton.TextScaled = true
        animationButton.Parent = scol

        corner(animationButton, 1)
        st(animationButton, 2, 0, 0, 0)

        animationButton.MouseButton1Click:Connect(function()
                if isPlaying then
                        for _, anim in ipairs(animations) do
                                if anim.Track then
                                        anim.Track:Stop()
                                end
                        end
                        isPlaying = false
                        animationButton.Text = nam
                else
                        for _, anim in ipairs(animations) do
                                local animationInstance = Instance.new("Animation")
                                animationInstance.AnimationId = anim.Id
                                anim.Track = humanoid:LoadAnimation(animationInstance)
                                anim.Track.Looped = true
                                anim.Track:Play()
                        end
                        isPlaying = true
                        animationButton.Text = "Stop " .. nam

                        task.delay(duration, function()
                                if isPlaying then
                                        for _, anim in ipairs(animations) do
                                                if anim.Track then
                                                        anim.Track:AdjustSpeed(sp)
                                                end
                                        end
                                end
                        end)
                end
        end)

        game.Players.LocalPlayer.CharacterAdded:Connect(function()
                humanoid = game.Players.LocalPlayer.Character:WaitForChild("Humanoid")
                -- Pastikan animasi dihentikan saat karakter respawn
                for _, anim in ipairs(animations) do
                        if anim.Track then
                                anim.Track:Stop()
                        end
                end
                isPlaying = false
                animationButton.Text = nam
        end)
end

se("Sex 1(fr)", "182789003", "0", "0", "0", 1.8,0)
se("Sex 2(fr)", "182789003", "106772613", "0", "0", 1.8,0)
se("sex 3(fr)","182789003","182749109","0","0","1.9","0")
se("sex 4(fr)","","179224234","182789003","182749109","0.1","0")
se("bang 1(be)", "148840371", "", "0", "0", 0,3)
se("bang 2(be)", "148840371", "95383980", "0", "0", 0,3)
se("bang 3(be)","182789003","158821881","106772613","148840371","0","5")
se("Lay down bang(be)", "148840371", "182749109", "0", "0", 0,2)
se("Lay Down sex(fr)", "182724289", "106772613", "181526230", "0", 0,1)
se("Lay Down sex 2(fr)", "182724289", "179224234", "181526230", "0", 0,1)
se("BlowJob V1(fr)","182749109","182789003","0","0","0","5")

local front4 = Instance.new("TextButton")
front4.Size = UDim2.new(0.1, 0, 0.1, 0)
front4.Position = UDim2.new(0, 0, 0.2, 0)
front4.BackgroundColor3 = Color3.new(0.8, 0.4, 0)
front4.BorderColor3 = Color3.new(0, 0, 0)
front4.BorderSizePixel = 1
front4.Text = "Front v4"
front4.TextColor3 = Color3.new(255, 255, 255)
front4.BackgroundTransparency = 0
front4.Font = Enum.Font.FredokaOne
front4.TextScaled = true
front4.Parent = frame2
corner(front4, 0.3)

local player = game.Players.LocalPlayer
local teleportSpeed = 10
local isTeleporting = false
local targetPlayer = nil
local offset = CFrame.new(0, 1.2, -1)
local toggleInterval = 0.3

local function getTargetPlayerByName(name)
    for _, p in ipairs(game.Players:GetPlayers()) do
        if p.Name:sub(1, #name) == name or p.DisplayName:sub(1, #name) == name then
            return p
        end
    end
    return nil
end

local function startTeleport()
    if targetPlayer and targetPlayer.Character then
        local targetCharacter = targetPlayer.Character
        local targetRootPart = targetCharacter:FindFirstChild("HumanoidRootPart")
        
        if targetRootPart then
            local teleportLoop
            teleportLoop = game:GetService("RunService").Stepped:Connect(function()
                if isTeleporting then
                    -- Teleport player while rotating them to face backward
                    local targetCFrame = targetRootPart.CFrame * offset
                    player.Character:SetPrimaryPartCFrame(targetCFrame * CFrame.Angles(0, math.rad(180), 0))
                else
                    teleportLoop:Disconnect()
                end
            end)

            -- Toggle offset every 'toggleInterval' seconds
            local lastToggleTime = tick()
            local toggleOffset
            toggleOffset = game:GetService("RunService").Heartbeat:Connect(function()
                if isTeleporting then
                    if tick() - lastToggleTime >= toggleInterval then
                        if offset == CFrame.new(0, 1.2, -1) then
                            offset = CFrame.new(0, 1.2, -1)
                        else
                            offset = CFrame.new(0, 1.2, -1)
                        end
                        lastToggleTime = tick()
                    end
                else
                    toggleOffset:Disconnect()
                end
            end)
        end
    end
end

front4.MouseButton1Click:Connect(function()
    local targetName = namebox.Text
    targetPlayer = getTargetPlayerByName(targetName)

    if isTeleporting then
        isTeleporting = false
        front4.Text = "Front v4"
    else
        isTeleporting = true
        front4.Text = "Stop"
        startTeleport()
    end
end)
